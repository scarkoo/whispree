[project]
name = "whispree-mlx-worker"
version = "0.1.0"
description = "Pinned local MLX workers for Whispree"
requires-python = ">=3.11"
dependencies = [
    "mlx-audio==0.4.3",
    "mlx-lm==0.31.3",
    "soundfile==0.12.1",
    "numpy==2.0.0",
]

[tool.uv]
dev-dependencies = []
prerelease = "allow"
